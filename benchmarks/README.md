# BBM benchmark snapshot

`BBM/` contains 230 Boolean networks from the Biodivine Boolean Models
repository snapshot used by the study. Exactly 218 networks have a matching
`model_NNN_01.csv` fixed-point target; these 218 pairs form the immutable
experimental set. The remaining 12 networks are retained so that the selection
criterion is transparent.
